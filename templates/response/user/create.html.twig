{% extends 'question/base.html.twig' %}
{% block title %}{{username}} | Créer une Question{% endblock %}
{% block body %}
    {% include 'partials/flash.html.twig' %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center"><h1>Repondre a une question</h1>
                    {{ form_start(form, {'attr': {'class': 'form-response'}}) }}
                    {{ form_row(form.question) }}
                    {{ form_row(form.content) }}
                    <div class="text-center">
                        {{ form_row(form.submit) }}
                    </div>
                    {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
